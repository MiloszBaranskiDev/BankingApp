export enum ERoute {
  home = "/",
  wallet = "/wallet",
  currencies = "/currencies",
  transfer = "/transfer",
  transactions = "/transactions",
  help = "/help",
  settings = "/settings",
  profile = "/profile",
  faq = "/faq",
  chat = "/chat",
  notifications = "/notifications",
  notification = "/notification/",
}
